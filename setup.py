__author__ = 'Ibrahim'

from setuptools import setup

setup(
    name = "PersonalizedGroupMail",
    version = "0.0",
    author = "Ibrahim Ahmed",
    description = "Send batch emails with personalized greetings.",
    packages = ['PersonalizedGroupMail'],
    install_requires = ['html2text']
)
